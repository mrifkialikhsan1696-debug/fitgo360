# For production auth, replace mock auth with real JWT secret
JWT_SECRET="change-me"

# For real rate-limit storage (Upstash), if you upgrade later
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
